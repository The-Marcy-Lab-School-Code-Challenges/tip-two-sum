{
  "name": "tip-two-sum",
  "version": "1.0.0",
  "private": true,
  "description": "Two Sum coding challenge with Jest tests",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/?(*.)+(test).[jt]s?(x)"],
    "collectCoverage": false
  }
}
